Good. `job_parser.py` and `job_embedder.py` don't exist yet. `JobMarkdownCreateView` also doesn't exist. Starting with the parser:Parser is green. Now the embedder:Now I have the full picture. The embedder should use `EmbeddingClient` from `resume_pipeline/embeddings.py`:Now add `JobMarkdownCreateView` and wire the route. Let me read the existing views and urls files:Now I'll add `JobMarkdownCreateView`. I also need to check if `JobDetailView.patch` should re-trigger embeddings — the plan says it should, so I need to update that too:Now update `JobDetailView.patch` to re-trigger embeddings, and update `JobDetailView.delete` to not call embeddings (it already doesn't — just verify):Now add `JobMarkdownInputSerializer` to the serializers module:Now I have everything I need. Adding the serializer and wiring the URL:Now verify views.py has the `JobMarkdownCreateView` and the correct imports:Good. Now do a quick smoke-check to confirm imports resolve and the URL route is registered correctly:Task #99 is done. Now writing the parser unit tests (Task #100):Now I have the full test code from the plan. Let me write the parser tests file:Now verify the test file runs correctly using the pure Python approach (since Django ORM isn't needed for these pure-function tests):Two issues to fix: `assert_` ordering in the script (harness-only bug), and the `invalid_must_haves_yaml` test case uses input that hits the `":" in stripped` branch before the `startswith` check, so it's silently ignored. Let me fix the test to use input that actually reaches the else-branch guard:26/26. Now Task #101 — the BDD feature file and step definitions. Let me pull the plan's Gherkin:I have the full picture. Now let me write the BDD feature file and step definitions:Now write the step definitions. The pattern used by existing step files (like `dashboard_stats_steps.py`) is to mock ORM entirely and drive views through `APIRequestFactory`, so I'll follow the same approach:Now verify the step definitions parse correctly (no syntax errors, scenario count matches):All green. Here's what was completed this session:

**Task #99** — `JobMarkdownCreateView` fully wired: `JobMarkdownInputSerializer` added to `serializers.py`, import + route `POST /api/jobs/markdown/` added to `urls.py` (ordered before `jobs/<uuid:pk>/` to avoid routing conflict).

**Task #100** — `tests/unit/test_job_parser.py` written with 26 cases across 4 test classes: `TestParserHappyPath` (13 cases verifying JOB_SPEC alignment), `TestParserEdgeCases` (5 cases for whitespace/casing/bullets), `TestParserRejection` (6 cases for malformed inputs), and `TestJobSpecDataclass` (3 contract checks). All 26 pass against the pure-function parser.

**Task #101** — `features/job_lifecycle.feature` (16 scenarios across 3 groups: creation, error handling, CRUD) and `features/steps/job_lifecycle_steps.py` (full step definitions, all ORM-mocked via `APIRequestFactory`, same pattern as existing BDD step files).